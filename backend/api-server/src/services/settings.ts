// Business logic for the settings module lives here; routes stay thin.
// Stub until the module is wired to real tables.
export function getModuleStatus() {
  return { module: "settings", status: "stub" } as const;
}
