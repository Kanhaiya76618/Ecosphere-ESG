{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && PORT=5173 BASE_PATH=/ pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./frontend/**\" --filter \"./backend/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:frontend": "PORT=5173 BASE_PATH=/ pnpm --filter @workspace/ecosphere dev",
    "dev:backend": "DATABASE_URL=postgres://cognee:cognee@localhost:5432/ecosphere PORT=5050 pnpm --filter @workspace/api-server dev"
  },
  "private": true,
  "dependencies": {
    "@esbuild/darwin-arm64": "0.27.3",
    "@replit/connectors-sdk": "^0.4.1",
    "@rollup/rollup-darwin-arm64": "4.62.2",
    "@tailwindcss/oxide-darwin-arm64": "4.3.2",
    "lightningcss-darwin-arm64": "1.32.0"
  },
  "devDependencies": {
    "prettier": "^3.9.4",
    "typescript": "~5.9.3"
  }
}
